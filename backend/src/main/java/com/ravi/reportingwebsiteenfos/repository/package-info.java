/**
 * Data access layer. Backed by in-memory data for this assessment, but shaped as
 * repository interfaces + implementations so a persistent store could be swapped in later.
 */
package com.ravi.reportingwebsiteenfos.repository;
